<h2>New book</h2>
<%= form_with model: book do |f| %>
  <div class="field">
    <h4>Title</h4>
    <%= f.text_field :title %>
    <h4>Opinion</h4>
    <%= f.text_area :body %>
      <div>
        <%= f.submit "Create Book", class: "btn btn-success" %>
      </div>
  </div>
<% end %>
